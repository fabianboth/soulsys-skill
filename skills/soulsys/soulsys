#!/usr/bin/env bash
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if command -v bun &> /dev/null; then
  exec bun "$SCRIPT_DIR/soulsys.js" "$@"
else
  exec node "$SCRIPT_DIR/soulsys.js" "$@"
fi
